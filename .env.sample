# This is a sample .env file for use in local development.
# Duplicate this file as .env in the root of the project
# and update the environment variables to match your
# desired config
#
# See the README for full descriptions of each of the
# available configurations.

PORT=3000
VITE_PUBLIC_GRAPHQL_URL=http://localhost:8080/v1/graphql
VITE_PUBLIC_GRAPHQL_WS=ws://localhost:8080/v1/graphql

VITE_PUBLIC_RPC_WEBSOCKET=http://localhost:26657/websocket
VITE_PUBLIC_CHAIN_STATUS=testnet
VITE_PUBLIC_REST_CHAIN_URL=http://localhost:1317

VITE_PUBLIC_IGNORE_LIST_URL=
